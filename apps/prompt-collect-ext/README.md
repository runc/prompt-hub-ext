# Prompt Collect Extension

- Content-script UI on X/Twitter profile pages
- Filter: recent days, keyword, max items, posts vs replies
- Captures full tweet text from X API responses (avoids relying on "Show more" DOM expansion)
- Optional: collect in background tab and return results
